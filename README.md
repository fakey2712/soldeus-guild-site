# SOL DEUS Website

Deployment-ready Next.js site for Vercel.

## Local setup

```bash
npm install
npm run dev
```

## Vercel setup

Add this environment variable in Vercel Project Settings → Environment Variables:

```bash
DISCORD_WEBHOOK_URL=your_discord_webhook_url
```

Then deploy and connect `soldeus.eu` in Project Settings → Domains.
